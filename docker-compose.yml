

services:
  nexora:
    build:
      context: .
      dockerfile: Dockerfile
    image: nexora-app
    container_name: nexora-container
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
